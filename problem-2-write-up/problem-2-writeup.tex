
% push the following commands to make sure the .tex is compiled into pdf (without the %)
% git add .github/workflows/latex.yml p2/
% git commit -m "Add LaTeX workflow and files"
% git push origin main